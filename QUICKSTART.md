# Быстрый старт

## Минимальная настройка для запуска

### 1. Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Создание файла .env

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Минимальные настройки:

```env
DATABASE_URL=sqlite:///./vpn_billing.db
BOT_TOKEN=your_bot_token_here
ADMIN_TELEGRAM_IDS=123456789
TELEGRAM_BOT_NAME=your_bot_username
```

### 3. Инициализация базы данных

```bash
# Создание таблиц
python -c "from app.database import Base, engine; Base.metadata.create_all(bind=engine)"

# Или через Alembic
alembic upgrade head

# Заполнение тестовыми данными (опционально)
python scripts/init_db.py
```

### 4. Запуск

#### Вариант 1: Docker Compose (рекомендуется)

```bash
docker-compose up -d
```

#### Вариант 2: Вручную

В двух терминалах:

**Терминал 1 - Backend API:**
```bash
uvicorn app.main:app --reload --port 8000
```

**Терминал 2 - Telegram Bot:**
```bash
python -m app.bot.main
```

### 5. Проверка работы

1. Откройте http://localhost:8000 - веб-интерфейс
2. Откройте http://localhost:8000/docs - API документация
3. Отправьте `/start` в Telegram боту

## Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env` файл

## Получение Telegram ID

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID в `ADMIN_TELEGRAM_IDS` в `.env`

## Импорт пользователей из CSV

1. Войдите в админ-панель: http://localhost:8000/admin
2. Введите ваш Telegram ID
3. Перейдите на вкладку "Импорт CSV"
4. Загрузите файл в формате (пример в `examples/users_example.csv`):
   ```
   telegram_id;name;start_date;balance;key_data
   123456789;Иван Иванов;15.01.2024;250.00;vless://...
   ```

## Основные команды бота

- `/start` - Регистрация/вход
- `/admin` - Админ-панель (только для админов)
- `👤 Мой профиль` - Просмотр профиля
- `🔑 Получить ключ` - Получение VPN ключа
- `💰 Пополнить баланс` - Реквизиты для оплаты

## Устранение проблем

**Бот не отвечает:**
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь, что бот запущен

**Ошибка подключения к БД:**
- Для SQLite: проверьте права на запись в директории
- Для PostgreSQL: проверьте `DATABASE_URL` и доступность сервера

**Планировщик не работает:**
- Планировщик запускается автоматически с ботом
- Проверьте логи бота


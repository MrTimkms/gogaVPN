# 🔄 Переустановка проекта

## Вариант 1: Быстрая переустановка (сохраняет .env)

Если нужно просто обновить код и пересобрать контейнеры:

```bash
cd ~/gogaVPN

# Остановить и удалить контейнеры
docker compose down

# Обновить код
git pull

# Пересобрать и запустить
docker compose build --no-cache
docker compose up -d
```

## Вариант 2: Полная переустановка (удаляет всё)

### Автоматическая переустановка:

```bash
cd ~/gogaVPN
chmod +x reinstall.sh
./reinstall.sh
```

Скрипт:
- ✅ Остановит и удалит все контейнеры
- ✅ Удалит старые образы
- ✅ Обновит код из GitHub
- ✅ Пересоберет контейнеры
- ✅ Запустит всё заново

### Ручная переустановка:

```bash
cd ~/gogaVPN

# 1. Остановить и удалить всё (включая данные БД)
docker compose down -v

# 2. Удалить старые образы
docker rmi gogavpn-backend gogavpn-bot

# 3. Обновить код
git pull

# 4. Пересобрать
docker compose build --no-cache

# 5. Запустить
docker compose up -d
```

## Вариант 3: Установка с нуля

Если хотите начать полностью заново:

```bash
# Удалить старую версию
cd ~
rm -rf ~/gogaVPN

# Клонировать заново
git clone https://github.com/MrTimkms/gogaVPN.git
cd gogaVPN

# Запустить установку
chmod +x fresh_install.sh
./fresh_install.sh
```

Или вручную:

```bash
cd ~
rm -rf ~/gogaVPN
git clone https://github.com/MrTimkms/gogaVPN.git
cd gogaVPN
chmod +x setup.sh
./setup.sh
docker compose up -d --build
```

## ⚠️ Важно!

**При использовании `docker compose down -v`:**
- ❌ Удаляются **все данные** из базы данных
- ❌ Удаляются **все пользователи**
- ❌ Удаляются **все транзакции**

**Если нужно сохранить данные:**
```bash
# Создать бэкап перед удалением
docker compose exec db pg_dump -U vpn_user vpn_billing > backup_$(date +%Y%m%d).sql

# Восстановить после переустановки
docker compose exec -T db psql -U vpn_user vpn_billing < backup_20240101.sql
```

## 🔍 Проверка после переустановки

```bash
# Статус контейнеров
docker compose ps

# Логи
docker compose logs -f

# Проверка веб-интерфейса
curl http://localhost:8085
```

## ❓ Проблемы?

Если что-то не работает после переустановки:

1. Проверьте `.env` файл:
   ```bash
   cat .env
   ```

2. Проверьте логи:
   ```bash
   docker compose logs
   ```

3. Пересоберите без кэша:
   ```bash
   docker compose build --no-cache
   docker compose up -d
   ```


# 🐳 Простая установка через Docker

Этот проект настроен для автоматической установки через Docker. Вам не нужно вручную устанавливать Python, PostgreSQL или другие зависимости - Docker сделает всё за вас!

## 📋 Что нужно установить

Только **Docker Desktop**:
- **Windows/Mac**: [Скачать Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: `sudo apt install docker.io docker-compose` или следуйте [официальной инструкции](https://docs.docker.com/engine/install/)

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Клонирование проекта

```bash
gh repo clone MrTimkms/gogaVPN
cd gogaVPN
```

### Шаг 2: Автоматическая настройка

**Windows:**
```bash
setup.bat
```

**Linux/Mac:**
```bash
chmod +x setup.sh
./setup.sh
```

Скрипт **интерактивно запросит** у вас необходимые данные:

1. **🤖 BOT_TOKEN** - токен бота от @BotFather
2. **👤 ADMIN_TELEGRAM_IDS** - ваш Telegram ID  
3. **📝 TELEGRAM_BOT_NAME** - имя бота (опционально)

**Как получить данные:**

1. **BOT_TOKEN**: 
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **ADMIN_TELEGRAM_IDS**:
   - Напишите [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте команду `/start`
   - Скопируйте ваш ID (число, например `123456789`)

3. **TELEGRAM_BOT_NAME**:
   - Имя вашего бота без символа @
   - Например, если бот называется `@my_vpn_bot`, введите `my_vpn_bot`
   - Можно пропустить (нажать Enter)

Все данные **автоматически сохранятся** в файл `.env`, и контейнеры запустятся!

## 🎯 Готово!

Откройте в браузере:
- 🌐 **Веб-интерфейс**: http://localhost:8000
- 📚 **API документация**: http://localhost:8000/docs
- ⚙️ **Админ-панель**: http://localhost:8000/admin

## 📊 Полезные команды

### Просмотр логов
```bash
# Все сервисы
docker compose logs -f

# Только бот
docker compose logs -f bot

# Только API
docker compose logs -f backend
```

### Остановка
```bash
docker compose down
```

### Перезапуск
```bash
docker compose restart
```

### Остановка и удаление данных
```bash
docker compose down -v
```

### Обновление проекта
```bash
git pull
docker compose up -d --build
```

## 🔧 Ручной запуск (если скрипт не работает)

### 1. Создайте .env файл

Скопируйте `env.example.txt` в `.env`:
```bash
# Windows
copy env.example.txt .env

# Linux/Mac
cp env.example.txt .env
```

### 2. Отредактируйте .env

Укажите `BOT_TOKEN`, `ADMIN_TELEGRAM_IDS`, `TELEGRAM_BOT_NAME`

### 3. Запустите Docker

```bash
docker compose up -d --build
```

### 4. Проверьте статус

```bash
docker compose ps
```

Все сервисы должны быть в статусе "Up"

## ❓ Решение проблем

### Docker не запускается
- Убедитесь, что Docker Desktop запущен
- Проверьте: `docker --version`

### Ошибка "port already in use"
- Порт 8000 занят? Измените в `docker-compose.yml`: `"8001:8000"`

### Бот не отвечает
- Проверьте `BOT_TOKEN` в `.env`
- Посмотрите логи: `docker compose logs bot`

### База данных не подключается
- Подождите 10-15 секунд после запуска (БД инициализируется)
- Проверьте логи: `docker compose logs db`

### Нужно сбросить базу данных
```bash
docker compose down -v
docker compose up -d
```

## 📦 Что установлено автоматически

Docker автоматически устанавливает и запускает:
- ✅ Python 3.11
- ✅ PostgreSQL 15
- ✅ Все Python зависимости из `requirements.txt`
- ✅ FastAPI сервер
- ✅ Telegram бот
- ✅ Планировщик задач

Всё работает в изолированных контейнерах - не засоряет вашу систему!

## 🎓 Дополнительная информация

- Полная документация: `README.md`
- Развертывание на сервере: `DEPLOYMENT.md`
- Быстрый старт: `QUICKSTART.md`


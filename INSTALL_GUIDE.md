# 📖 Пошаговая инструкция по установке

## ✅ Шаг 1: Docker установлен

Отлично! Docker Desktop установлен и запущен.

## 📥 Шаг 2: Клонирование проекта

Если вы еще не клонировали проект, выполните:

```bash
gh repo clone MrTimkms/gogaVPN
cd gogaVPN
```

Или если используете обычный git:
```bash
git clone https://github.com/MrTimkms/gogaVPN.git
cd gogaVPN
```

## 🚀 Шаг 3: Запуск установки

### Для Windows:

1. Откройте **PowerShell** или **Командную строку** в папке проекта
2. Запустите:
   ```bash
   setup.bat
   ```

### Для Linux/Mac:

1. Откройте терминал в папке проекта
2. Сделайте скрипт исполняемым и запустите:
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```

## 📝 Шаг 4: Ввод данных

Скрипт попросит вас ввести:

### 1. BOT_TOKEN (обязательно)

**Как получить:**
1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: "My VPN Bot")
   - Введите username бота (например: "my_vpn_bot" - должен заканчиваться на `bot`)
5. BotFather пришлет вам токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Скопируйте этот токен** и вставьте в скрипт

### 2. ADMIN_TELEGRAM_IDS (обязательно)

**Как получить:**
1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте команду `/start`
4. Бот покажет ваш ID (это число, например: `123456789`)
5. **Скопируйте это число** и вставьте в скрипт

### 3. TELEGRAM_BOT_NAME (опционально)

- Это имя вашего бота без символа @
- Например, если бот называется `@my_vpn_bot`, введите `my_vpn_bot`
- Можно пропустить (нажать Enter)

## ⏳ Шаг 5: Ожидание запуска

После ввода данных скрипт:
- ✅ Создаст файл `.env` с вашими настройками
- ✅ Запустит Docker контейнеры
- ✅ Установит все зависимости
- ✅ Настроит базу данных

Это займет **2-5 минут** при первом запуске.

## ✅ Шаг 6: Проверка работы

После завершения установки откройте в браузере:

- 🌐 **Веб-интерфейс**: http://localhost:8000
- 📚 **API документация**: http://localhost:8000/docs
- ⚙️ **Админ-панель**: http://localhost:8000/admin

## 🎉 Готово!

Теперь вы можете:
- Использовать веб-интерфейс для управления
- Отправить `/start` в Telegram боту
- Импортировать пользователей через админ-панель

## 📊 Полезные команды

### Просмотр логов
```bash
docker compose logs -f
```

### Остановка
```bash
docker compose down
```

### Перезапуск
```bash
docker compose restart
```

### Проверка статуса
```bash
docker compose ps
```

## ❓ Проблемы?

### Docker не запускается
- Убедитесь, что Docker Desktop запущен
- Проверьте: `docker --version`

### Порт 8000 занят
- Остановите другие приложения на порту 8000
- Или измените порт в `docker-compose.yml`

### Бот не отвечает
- Проверьте `BOT_TOKEN` в файле `.env`
- Посмотрите логи: `docker compose logs bot`

### Нужна помощь?
- Смотрите [DOCKER_SETUP.md](DOCKER_SETUP.md) для подробной информации
- Проверьте [README.md](README.md) для общей документации


#!/bin/bash

echo "🔍 Проверка порта 8000..."
echo ""

# Проверка, что использует порт 8000
if command -v lsof &> /dev/null; then
    echo "Процессы, использующие порт 8000:"
    sudo lsof -i :8000
elif command -v netstat &> /dev/null; then
    echo "Процессы, использующие порт 8000:"
    sudo netstat -tulpn | grep :8000
elif command -v ss &> /dev/null; then
    echo "Процессы, использующие порт 8000:"
    sudo ss -tulpn | grep :8000
else
    echo "⚠️ Не удалось найти утилиту для проверки порта"
fi

echo ""
echo "💡 Решения:"
echo "1. Остановить процесс, использующий порт 8000"
echo "2. Изменить порт в docker-compose.yml на другой (например, 8001)"
echo ""
read -p "Хотите изменить порт на 8001? (y/n): " CHANGE_PORT

if [[ "$CHANGE_PORT" =~ ^[Yy]$ ]]; then
    # Создание резервной копии
    cp docker-compose.yml docker-compose.yml.bak
    
    # Замена порта 8000 на 8001
    if [[ "$OSTYPE" == "darwin"* ]]; then
        sed -i '' 's/"8000:8000"/"8001:8000"/g' docker-compose.yml
    else
        sed -i 's/"8000:8000"/"8001:8000"/g' docker-compose.yml
    fi
    
    echo "✅ Порт изменен на 8001"
    echo "Теперь проект будет доступен по адресу: http://ваш_сервер_ip:8001"
    echo ""
    echo "Запустите снова: docker compose up -d"
fi


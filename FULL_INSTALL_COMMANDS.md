# 🚀 Полная установка с нуля - Команды

## Скопируйте и выполните все команды по порядку:

```bash
# 1. Установка Docker (если не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh && sudo apt install docker-compose-plugin -y

# 2. Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# 3. Перезагрузка сессии (или выйдите и войдите заново)
newgrp docker

# 4. Удаление старой версии (если была)
rm -rf ~/gogaVPN

# 5. Клонирование проекта
cd ~ && git clone https://github.com/MrTimkms/gogaVPN.git && cd gogaVPN

# 6. Запуск установки
chmod +x setup.sh && ./setup.sh
```

## Или одной командой (автоматическая установка):

```bash
cd ~ && rm -rf ~/gogaVPN && git clone https://github.com/MrTimkms/gogaVPN.git && cd gogaVPN && chmod +x setup.sh && ./setup.sh
```

## Что произойдет:

1. Скрипт `setup.sh` запросит у вас:
   - 🤖 **BOT_TOKEN** (от @BotFather)
   - 👤 **ADMIN_TELEGRAM_IDS** (от @userinfobot)
   - 📝 **TELEGRAM_BOT_NAME** (опционально)

2. Автоматически:
   - ✅ Создаст файл `.env`
   - ✅ Запустит Docker контейнеры
   - ✅ Установит все зависимости
   - ✅ Настроит базу данных

## После установки:

```bash
# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f

# Откройте в браузере
# http://ваш_сервер_ip:8080
```

## Если Docker уже установлен:

```bash
# Просто выполните:
cd ~ && rm -rf ~/gogaVPN && git clone https://github.com/MrTimkms/gogaVPN.git && cd gogaVPN && chmod +x setup.sh && ./setup.sh
```

